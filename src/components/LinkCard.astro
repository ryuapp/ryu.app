---
const frontmatter = Astro.props;
---

<a
  href={frontmatter.href}
  class="mx-2 my-1 w-60 rounded-md border border-solid border-gray-200 bg-transparent p-2 cursor-pointer scale-custom hover:bg-gray-100 active:bg-zinc-200"
>
  <dl>
    <dt class="text-lg">
      {frontmatter.title}
    </dt>
    <dd class="text-sm text-zinc-500">
      {frontmatter.description}
    </dd>
  </dl>
</a>
